{
  "name": "agro-script-modding",
  "version": "1.0.0",
  "private": true,
  "description": "Site e API de pagamento/downloads protegidos para AGRO SCRIPT MODDING.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check admin.js && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0"
  }
}
